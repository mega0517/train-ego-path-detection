# 분기부 진로 유지를 위한 사전 경로 입력 채널: 출력 수준 시간 정련의 한계와 특징 수준 대안

**A Prior-Path Input Channel for Branch Persistence at Railway Turnouts: The Limits of Output-Level Temporal Refinement and a Feature-Level Alternative**

---

김백현¹ · 황현철¹†

¹ 한국철도기술연구원 (Korea Railroad Research Institute)
주저자: 김백현 (bhkim@krri.re.kr)
† 교신저자: 황현철 (hchwang@krri.re.kr)

**Baek-Hyun Kim¹ · Hyeon-Chyeol Hwang¹†**

---

## 국문 초록

철도 자기 경로(ego-path) 검출은 분기부에서 가장 어렵다. 열차가 분기기에 진입하면 진로를 결정하는 텅레일 밀착부가 차체 전두부에 가려 시야에서 사라지고, 그 구간의 영상만으로는 좌·우 어느 선로가 자기 경로인지 원리적으로 판별할 수 없다. 본 논문은 이 실패가 정확도 저하가 아니라 **이산적 진로 뒤바뀜**임을 정량적으로 규명하고, 그 원인이 모델 용량이 아니라 **직전 판단을 보존할 상태의 부재**임을 보인 뒤, 상태를 특징 수준에 주입하는 **사전 경로 입력 채널(prior-path channel)**을 제안한다. 제안 방법은 직전 프레임에서 채택한 경로를 마스크로 렌더링해 RGB에 이어 붙인 4번째 입력 채널로 공급하며, 해당 채널의 첫 합성곱 가중치를 영으로 초기화하여 학습 시작 시점의 모델이 프레임 단위 기준 모델과 **정확히 동일**하도록 한다. 학습에서는 사전정보를 세 가지 상태(빈 사전정보, 크게 어긋난 사전정보, 소폭 흔들린 정답)로 의도적으로 손상시키고 RGB 하단 밴드를 확률적으로 가려, 분기 통과 기하를 합성한다. 이 손상 혼합이 없으면 모델은 사전정보를 그대로 복사하는 적분기로 수렴하여 잘못된 진로에서 복귀하지 못한다. 평가는 라벨이 부여된 분기 통과 이벤트 28개(2,268 프레임)에서 수행하며, IoU와 함께 원거리 진로 이탈을 직접 세는 **오분기 프레임률**과 **에피소드 수**를 보고한다. 기준 모델은 IoU 0.9213으로 양호해 보이지만 28개 이벤트 중 7개에서 오분기 에피소드를 일으키며 최장 8프레임(2초) 동안 잘못된 선로를 가리켰다. 즉 IoU는 이 실패를 가린다. 【결과 요약은 실험 후 확정】 부수적으로 본 연구는 선행 연구에서 확인된 출력 수준 순환 정련부의 항등 수렴 현상에 대한 구조적 설명을 제공한다: 129차원 출력 벡터 위의 순환 모듈은 좌표를 평활할 수는 있어도 증거의 가중을 바꿀 수 없으므로 이산 선택에 개입할 위치에 있지 않다.

**핵심어**: 자기 경로 검출, 철도 인지, 분기부, 사전정보 유도, 시간적 일관성, 가림 강건성

## Abstract

Railway ego-path detection is hardest at turnouts. As the train enters the switch, the tongue-rail closure that determines the route is occluded by the vehicle's front end and leaves the field of view; within that window the imagery cannot, in principle, disambiguate which of the two tracks is the ego-path. This paper shows quantitatively that the resulting failure is not a degradation in accuracy but a **discrete branch reversal**, that its cause is not model capacity but the **absence of any state carrying the earlier decision**, and proposes a **prior-path input channel** that injects that state at the feature level. The previously accepted path is rendered as a mask and appended to the RGB input as a fourth channel, whose first-convolution weights are zero-initialised so that the model at the start of training is *exactly* the per-frame baseline. During training the prior is deliberately corrupted into three regimes (empty, badly displaced, and lightly jittered ground truth) while the bottom band of the RGB is stochastically blanked, synthesising the switch-passage geometry. Without this mixture the model converges to an integrator that copies the prior and can never recover from a wrong branch. Evaluation uses 28 labelled turnout events (2,268 frames) and reports, alongside IoU, a **wrong-branch frame rate** and an **episode count** that measure far-field branch departure directly. The baseline model looks healthy at IoU 0.9213 yet produces wrong-branch episodes in 7 of 28 events, pointing at the wrong track for up to 8 frames (2 s): IoU hides the failure. 【Results to be finalised after the experiments.】 As a by-product the paper offers a structural explanation for the identity collapse of output-level recurrent refiners reported previously: a recurrent module on a 129-dimensional output vector can smooth coordinates but cannot reweight evidence, and so is not positioned to intervene in a discrete choice.

**Keywords**: ego-path detection, railway perception, turnout, prior-guided detection, temporal consistency, occlusion robustness

---

## 1. 서론

철도 자율주행과 운전 지원에서 자기 경로 검출은 전방 장애물 판정의 전제가 된다. 검출된 경로가 실제 주행 선로와 다르면 그 위의 모든 판단이 잘못된 영역을 대상으로 하게 되므로, 경로의 **정확도**보다 **정합성**이 더 중요한 상황이 존재한다. 분기부가 정확히 그런 상황이다.

분기기 통과는 세 국면으로 나뉜다. 접근 국면에서는 텅레일 밀착부가 화면에 보이고 진로가 결정 가능하다. 통과 국면에서는 그 밀착부가 화면 하단으로 내려가 차체 전두부에 가리며, 이때 영상에는 좌·우 두 선로가 거의 대칭으로 남는다. 이탈 국면에서는 차체가 실제로 방향을 틀어 다시 판별이 가능해진다. 문제는 가운데 국면이다. 판단 근거가 이미 화면을 떠났고 아직 차체 거동으로 드러나지 않은 이 구간에서, 프레임 단위 모델은 **원리적으로** 진로를 알 수 없다.

선행 연구[1]는 이 구간을 순환 신경망으로 보완할 것을 제안하였으나 시계열 데이터의 부재로 검증되지 못하였고, 저자들의 선행 작업[2]은 분기부 평가 데이터셋을 구축해 이를 검증한 결과 학습형 시간 정련이 **정확도가 아닌 안정성만** 제공하며 그 안정성마저 단순 지수이동평균(EMA)의 1/6 수준임을 보였다. 본 논문은 그 결과가 시간 정보 자체의 한계가 아니라 **정보를 주입한 위치의 한계**였음을 주장한다.

핵심 관찰은 다음과 같다. 분기부 실패는 좌표가 흔들리는 문제가 아니라 두 갈래 중 하나를 고르는 **이산 결정이 뒤집히는** 문제다(3장). 출력 벡터 위에서 동작하는 정련부나 이동평균은 이 결정에 개입할 수 없다. 평균은 양봉 분포에서 어느 쪽 선로도 아닌 중간 경로를 만들어 내며, 이는 물리적으로 존재하지 않는 경로다. 따라서 직전 판단은 좌표가 산출된 **이후**가 아니라 증거가 가중되는 **이전**, 즉 특징 추출 단계에 들어가야 한다.

본 논문의 기여는 다음과 같다.

1. 분기부 실패의 정량적 성격 규명. 실패는 프레임 간 이동량 기준 14~47배의 이산 점프로 나타나며, 80개 프레임 전이 중 단 2개에만 발생한다(3.1절). 즉 검출이 용이한 신호다.
2. IoU가 이 실패를 가린다는 사실의 실증과, 이를 직접 측정하는 **오분기 프레임률·에피소드 수** 지표의 제안(5.2절).
3. 사전 경로 입력 채널의 제안과, 영점 초기화 및 **사전정보 손상 학습**을 통해 이 채널이 적분기로 붕괴하지 않도록 하는 학습 절차(4장).
4. 출력 수준 순환 정련부가 항등에 수렴하는 현상에 대한 구조적 설명(7장).

## 2. 관련 연구

**철도 자기 경로 검출.** TEP-Net[1]은 자기 경로를 회귀·분류·분할 세 방식으로 정식화하고, 이전 프레임의 검출 결과로 다음 프레임의 관심 영역을 좁히는 자동 크롭(autocrop)을 제안하였다. 자동 크롭은 이미 일종의 시간적 사전정보이지만 그 정보는 **기하학적 관심 영역**에만 작용하며 크롭 창 내부의 진로 선택에는 관여하지 않는다. 본 논문의 채널은 같은 사전정보를 크롭이 아니라 **증거 가중**에 작용시킨다는 점에서 상보적이다.

**시간적 정련.** 저자들의 선행 작업[2]은 출력 벡터 위의 GRU 정련부(0.46 M 파라미터)를 학습하여 평가한 결과 사실상 항등 함수로 수렴함을 보고하였다. 본 논문 7장은 이 현상을 구조적으로 해석한다.

**가림 하 차선 검출.** 도로 차선 검출에서는 직전 프레임의 차선을 입력에 되먹이는 사전정보 유도(prior-guided) 방식이 가림·마모 구간의 표준적 해법으로 자리잡았다. 본 논문은 이 착상을 철도 분기부로 옮기되, 철도 특유의 요구—**잘못 커밋한 진로에서 반드시 복귀해야 한다**—를 학습 절차에 명시적으로 반영한다.

**다중 가설.** 분기 모호성을 이산 선택으로 재정식화하는 다중 가설 접근은 원리적으로 적합하나, 선행 검증[2]에서 가설 간 거리가 0.4 px 수준으로 붕괴하여 상한 성능조차 단일 경로를 넘지 못했다. 7.3절에서 이 실패와 본 방법의 관계를 논한다.

## 3. 문제 분석

### 3.1 실패는 이산적이다

분기 통과 이벤트 한 건(evt035, 81프레임, 4 fps)에 대해 분할 모델의 예측 경로가 원거리에서 화면 가로 방향 어디에 위치하는지를 프레임마다 측정하였다. 원거리 표본점은 예측 경로 자체의 세로 범위 상단 25% 지점으로 정의하여 크롭·해상도에 무관하게 비교 가능하도록 하였다.

분할 모델의 경우 프레임 간 이동량의 중앙값은 화면폭 대비 0.0038이었다. 이에 반해 진로가 우측에서 좌측으로 넘어가는 전이(#38→#39)는 0.055로 중앙값의 **14배**, 다시 우측으로 복귀하는 전이(#47→#48)는 0.178로 **47배**였다. 80개 전이 중 0.03을 넘는 것은 이 둘뿐이다. 회귀 모델도 같은 양상을 보였다(중앙값 0.0051, 최대 0.195 = 38배, 0.03 초과 2회).

그림 1이 이 측정을 요약한다. 이 분포는 두 가지를 말한다. 첫째, 실패는 점진적 표류가 아니라 이산 사건이다. 둘째, 신호 대 잡음비가 한 자릿수를 넘으므로 **검출 자체는 쉽다**. 어려운 것은 검출이 아니라, 검출한 뒤 무엇으로 대체할 것인가이다.

### 3.2 평균은 이 문제를 풀 수 없다

회귀 모델에 지수이동평균(α=0.5)을 적용하면 #39의 예측 위치는 0.496이 된다. 이때 원 예측은 좌측 분기의 0.473이고 직전 프레임(#38)까지 따르던 우측 분기는 0.514였으므로, 평활된 경로는 **두 선로의 정확히 사이**를 지난다. 평균은 양봉 사후분포에서 최빈값을 고르지 못하고 그 중간을 만들며, 그 중간은 어느 선로도 아니다.

정량적으로도 EMA는 뒤바뀜을 없애지 못한다. 최대 점프를 0.195에서 0.083으로 줄이지만 0.03을 넘는 전이 수는 2개로 그대로이며, 오분기 구간의 길이도 변하지 않는다. 이동평균은 지터를 줄이는 도구이지 결정을 내리는 도구가 아니다.

### 3.3 정보는 이미 화면을 떠났다

오분기가 유지된 구간(#39~#47, 4 fps 기준 약 2.3초)에서 진로를 결정하는 텅레일 밀착부는 어떤 입력에도 존재하지 않는다. 판단 근거는 약 10프레임 전에 화면에 있었다. 그러므로 이것은 **표현력의 문제가 아니라 상태의 문제**다. 아무리 큰 모델도 없는 정보를 복원할 수 없으며, 필요한 것은 과거의 판단을 현재로 옮기는 경로다.

### 3.4 시야를 넓히는 것은 해답이 아니다

동일 이벤트의 크롭 전 원본(1280×720)에 같은 모델을 적용하면 0.03을 넘는 전이가 2개에서 1개로 줄고(남는 하나는 #47→#48의 실제 진로 변경이다) 오분기 구간이 사라진다. 그러나 이는 문제를 푼 것이 아니다. 예측 경로의 세로 범위가 0.597에서 0.426으로, 원거리 마스크 폭이 0.102에서 0.044로 줄었다. 모델이 **멀리 약속하지 않기 때문에** 틀릴 일도 없어진 것이며, 전방 유도라는 목적 자체를 포기한 결과다.

## 4. 제안 방법: 사전 경로 입력 채널

### 4.1 채널 구성

시각 t의 입력을 RGB 3채널과 사전정보 1채널을 이어 붙인 4채널 텐서로 구성한다.

$$\mathbf{x}_t = [\, \mathbf{I}_t \;;\; \mathbf{P}_t \,] \in \mathbb{R}^{4 \times H \times W}$$

여기서 $\mathbf{P}_t$는 시각 $t-1$에 채택된 경로 $\hat{y}_{t-1}$를 좌·우 레일 사이를 채운 이진 마스크로 렌더링한 것이다. 사전정보는 **원본 영상 좌표계**에 보관하고 매 프레임 현재 크롭 창으로 다시 투영한다. 자동 크롭 창이 프레임마다 움직이므로, 크롭 좌표계에 보관하면 사전정보가 자신이 설명하는 영상에 대해 조용히 어긋나게 된다.

### 4.2 영점 초기화

4번째 채널에 대응하는 첫 합성곱 가중치를 0으로 초기화한다. RGB 필터는 사전학습 가중치를 그대로 복사한다. 이렇게 하면 학습 시작 시점의 4채널 모델은 기준 3채널 모델과 **수치적으로 동일한 함수**를 계산한다.

이 선택은 단순한 안정화 기법이 아니라 비교의 공정성에 관한 것이다. 무작위 초기화된 4번째 채널은 학습 초기에 잡음을 주입하며, 초기 몇 에폭은 그 잡음을 상쇄하는 데 소비된다. 영점 초기화는 모델이 사전정보에 대한 의존을 **처음부터 획득해야 하도록** 만들고, 따라서 최종 성능 차이를 채널의 정보 가치에만 귀속시킬 수 있게 한다. 동일한 착상이 선행 작업[2]의 순환 정련부 출력 영점 초기화에도 쓰였다.

### 4.3 사전정보 손상 학습

정답 경로를 그대로 사전정보로 주면 학습은 사소한 해로 붕괴한다. 사전정보를 복사하기만 하면 손실이 0이 되므로 모델은 영상을 보지 않는 적분기가 되고, 추론 시 한 번 잘못 커밋한 진로에서 영원히 복귀하지 못한다. 이는 철도에서 특히 위험하다. 3.1절에서 보았듯 실제 진로는 이탈 국면에서 반드시 바뀌기 때문이다.

따라서 사전정보를 세 가지 상태로 확률적으로 손상시킨다.

| 상태 | 확률 | 내용 | 학습되는 것 |
|---|---|---|---|
| 빈 사전정보 | 0.15 | 전부 0 | 시퀀스 시작·추적 상실 시 영상만으로 동작 |
| 크게 어긋남 | 0.15 | 폭의 6~25% 횡방향 이동 | 영상이 반박하는 사전정보를 **버릴 것** |
| 소폭 흔들림 | 0.70 | σ=0.02·W 정규 이동 | 한 프레임의 자기 운동에 해당하는 변형 흡수 |

이와 **독립적으로** 확률 0.35로 RGB 하단 밴드(높이의 10~45%)를 0으로 만든다. 이것이 분기 통과 기하의 합성이다. 하단이 가려지고 사전정보는 정확한 표본에서 모델은 "진로를 유지하라"를 배우고, 하단이 보이는데 사전정보가 어긋난 표본에서 "사전정보를 버려라"를 배운다. 두 사분면이 모두 있어야 유지와 복귀를 함께 얻는다.

목표값은 손상 여부와 무관하게 항상 현재 프레임의 정답이다. 손상은 입력에만 가한다.

### 4.4 추론

추론은 재귀적으로 동작한다. $\hat{y}_{t-1}$가 없는 시퀀스 첫 프레임에서는 빈 채널을 공급하며, 이는 학습에서 확률 0.15로 본 상태와 정확히 같다. 이후 매 프레임 직전 채택 결과를 렌더링해 공급하고, 시퀀스 경계에서 초기화한다.

## 5. 실험 설정

### 5.1 학습

학습 데이터는 OSDaR23 기반 자기 경로 라벨 1,535 프레임이다. 기준 모델과 제안 모델은 백본(ResNet-18, ImageNet 사전학습), 입력 해상도(512×512), 배치 크기(32), 학습률(1×10⁻⁴), 스케줄러(OneCycle), 에폭 수(250), 난수 시드(42)를 모두 동일하게 두고 **입력 채널 수만** 달리하여 학습하였다. 두 학습은 동일 호스트의 서로 다른 GPU에서 병렬로 수행하였다.

### 5.2 평가 지표

평가는 라벨이 부여된 분기 통과 이벤트 28개(각 81프레임, 총 2,268프레임)에서 수행한다. 각 이벤트는 독립 시퀀스로 처리하며 이벤트 경계에서 사전정보와 자동 크롭 상태를 초기화한다.

- **IoU**: 예측 경로 다각형과 라벨 다각형의 교집합 대 합집합.
- **오분기 프레임률**: 예측과 라벨의 **원거리 중심** 가로 위치 차이가 임계값 τ(화면폭의 5%)를 넘는 프레임의 비율. 원거리 표본점은 3.1절과 동일하게 정의한다.
- **에피소드 수**: 오분기 프레임이 2프레임 이상 연속된 최대 구간의 개수. 단발 잡음을 배제하고, 운전자가 실제로 인지할 만한 사건만 센다.
- **최장 에피소드**: 가장 긴 연속 오분기 길이(프레임).

IoU를 함께 보고하는 이유는 진로 안정성을 정확도로 사는 것을 막기 위해서다. 사전정보에 과의존하는 모델은 에피소드를 줄이면서 IoU를 잃을 수 있다.

### 5.3 IoU가 실패를 가린다는 증거

기존에 학습된 세 모델을 동일 평가셋에 적용한 결과는 표 1과 같다.

**표 1. 기존 모델의 IoU와 분기 신뢰성 (분기 통과 이벤트 28건, 2,268 프레임, τ=0.05)**

| 모델 | 방식 | IoU | 오분기 프레임 | 에피소드 | 최장(프레임) | 해당 이벤트 |
|---|---|---|---|---|---|---|
| twinkling-rocket-21 | 분할 | **0.9514** | 2.7% | 11 | **18** | 5 / 28 |
| chromatic-laughter-5 | 회귀 | 0.9213 | 2.4% | 14 | 8 | 7 / 28 |
| logical-tree-1 | 회귀 | 0.8983 | 5.2% | 18 | 21 | 11 / 28 |

두 지표는 서로를 예측하지 못한다. IoU가 가장 높은 twinkling-rocket-21(0.9514)이 **가장 긴 오분기 에피소드**를 만들며, 18프레임은 4 fps 기준 4.5초 동안 잘못된 선로를 자기 경로로 제시했다는 뜻이다. 반대로 IoU가 3점 낮은 chromatic-laughter-5의 최장 에피소드는 그 절반 이하다.

이유는 기하학적이다. 오분기 상태에서도 예측 경로와 라벨 경로는 근거리에서 거의 겹치며 원거리에서만 갈라지므로, 한 번의 에피소드가 IoU에 미치는 영향은 몇 점에 그친다. 평균 IoU 하나로 모델을 고르면 4.5초짜리 오분기를 가진 모델을 최고 성능으로 선정하게 된다. 오분기 지표를 따로 보고해야 하는 이유가 여기에 있다.

## 6. 결과

【학습 완료 후 확정】

## 7. 논의

### 7.1 왜 출력 수준 정련부는 항등에 수렴하는가

선행 작업[2]은 129차원 출력 벡터 위의 GRU 정련부가 사실상 항등 함수가 됨을 관측하였으나 그 이유는 규명하지 못했다. 본 논문의 분석은 구조적 설명을 제공한다.

출력 벡터는 이미 결정이 내려진 **후**의 표현이다. 그 위에서 할 수 있는 일은 좌표를 시간축으로 평활하는 것뿐이며, 3.2절에서 보았듯 평활은 양봉 분포에서 중간값을 만들어 손실을 오히려 키운다. 학습이 이를 발견하면 정련부는 아무것도 하지 않는 쪽을 선택한다. 즉 항등 수렴은 학습 실패가 아니라 **주어진 위치에서의 최적해**다. 진로 선택에 개입하려면 증거가 아직 가중되기 전, 특징 추출 단계에 정보가 들어가야 한다.

### 7.2 사전정보 유도와 적분기 붕괴의 경계

사전정보 채널은 양날의 검이다. 손상 없이 학습하면 적분기로 붕괴하고, 손상이 지나치면 채널이 무시된다. 4.3절의 혼합비는 이 두 실패 사이의 동작점을 정의한다. 특히 "크게 어긋남" 상태가 없으면 복귀 능력이 사라지는데, 철도 분기부에서 복귀는 선택이 아니라 요구사항이다.

### 7.3 다중 가설과의 관계

다중 가설 접근[2]은 분기 모호성을 이산 선택으로 다루려 했으나 가설이 붕괴하였다. 실패의 원인은 두 가지였다. 승자독식 손실만으로는 가설이 분리될 이유가 없었고, 선택이 프레임 단위여서 시간 정보가 개입하지 못했다. 사전정보 채널은 **선택을 명시적으로 표현하지 않고** 직전 선택을 증거로 환원한다는 점에서 다르며, 가설 분리라는 어려운 문제를 우회한다.

### 7.4 한계

【결과 확정 후 작성】

## 8. 결론

【결과 확정 후 작성】

## 참고문헌

[1] T. Laurent, "Train Ego-Path Detection on Railway Tracks Using End-to-End Deep Learning," arXiv:2403.13094, 2024.

[2] 김백현, 황현철, "철도 자기 경로 검출의 학습형 시간 정련은 이동평균 기준선을 능가하는가: 분기부 평가 데이터셋 구축과 통제 비교," 투고 중, 2026.

【추가 서지 보완 예정】
